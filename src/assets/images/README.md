# Menu Images

Add your own menu photos to this folder using these exact filenames:

- `burger.jpg`
- `fries.jpg`
- `icedtea.jpg`

The Express menu data in `server.js` already points to these Angular asset paths.
